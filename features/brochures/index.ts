// Brochures feature — branded share UX for the broker app (PRD §6.10).
// Activity writes go through @/features/engagement/actions logActivity.
export { BrochureShareClient } from "./components/BrochureShareClient";
